{
  "name": "ai-budget-firewall",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for OpenAI API hard stop-loss",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}